{
  "name": "full-stack-app",
  "version": "1.0.0",
  "description": "Full stack application",
  "dependencies": {
    "jwt-decode": "^4.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "start": "npm-run-all seed start:server build",
    "seed": "npm-run-all seed:server",
    "start:server": "cd server && npm start",
    "build": "cd client && npm run build"
  }
}
